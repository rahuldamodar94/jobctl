# jobctl reads NO secrets by default — the core scrape→match→triage loop and the
# local `claude` CLI features need zero env vars. These are ONLY needed if you
# configure an OpenAI-compatible LLM backend for the fit-judge / resume gen
# (profile.yaml: llm.backends.<name>.api_key_env points at one of these names).
# Copy to .env and fill the one you use. .env is gitignored.

# OPENAI_API_KEY=
# GEMINI_API_KEY=
# DEEPSEEK_API_KEY=
# OPENROUTER_API_KEY=

# Override the default port (3000) or DB path if you want:
# PORT=3000
# DB_PATH=./data/jobs.db
