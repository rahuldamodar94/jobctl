# Personal data — never committed
profile/

# Runtime data
data/

# Local agent/editor state (never publish session/lock/local-settings files)
.claude/

# Build & deps
node_modules/
dist/

# Misc
.DS_Store
*.log
.env

# stray root-level DBs (the real one lives in data/)
*.db
